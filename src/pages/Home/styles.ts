import styled from "styled-components";
import { Button } from "rsuite";

export const HomeButton = styled(Button)`
  display: block;
  width: 80%;
  margin: 10px auto;
  border-radius: 5px;
  font-weight: bold;
`;
